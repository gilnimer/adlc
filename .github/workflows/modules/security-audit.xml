<process name="security-audit" type="pipeline">
  <globals>
    <var name="code_to_audit" required="true" />
  </globals>

  <steps>
    <!-- Scan the code for vulnerabilities -->
    <step id="scan" agent="security-scanner">
      <input format="text">
        Perform a security audit on the following code:
        ${{code_to_audit}}

        Check for OWASP Top 10 vulnerabilities:
        1. Injection
        2. Broken Authentication
        3. Sensitive Data Exposure
        4. XML External Entities
        5. Broken Access Control
        6. Security Misconfiguration
        7. Cross-Site Scripting (XSS)
        8. Insecure Deserialization
        9. Using Components with Known Vulnerabilities
        10. Insufficient Logging
      </input>
      <output save_as="scan_results" />
      <routing>
        <on-status value="success" goto="report" />
        <on-status value="fail" goto="report" />
        <on-error goto="report" retry_count="2" />
      </routing>
    </step>

    <!-- Generate the final report -->
    <step id="report" agent="security-reporter">
      <input format="text">
        Generate a security audit report from these scan results:
        ${{scan_results}}
      </input>
      <output save_as="audit_report" />
    </step>
  </steps>
</process>