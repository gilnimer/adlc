// CopilotLLMClient has been moved to @aoml/action.
// @aoml/core is environment-agnostic — it only defines the LLMClient interface
// in adapter.ts. Provider implementations are injected by wrapper packages.
// Per TSD §8: "The Engine is completely environment-agnostic via Dependency Injection."
