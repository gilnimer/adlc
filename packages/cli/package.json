{
  "name": "@aoml/cli",
  "version": "0.0.1",
  "description": "AOML CLI - local workflow execution",
  "type": "module",
  "bin": {
    "aoml": "./dist/bin.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest run"
  },
  "dependencies": {
    "@aoml/core": "workspace:*"
  }
}
